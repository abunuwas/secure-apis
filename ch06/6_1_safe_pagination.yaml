paths:
  /products:
    get:
      parameters:
        - name: page
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
        - name: perPage
          in: query
          required: false
          schema:
            type: integer
            minimum: 10
            maximum: 100
